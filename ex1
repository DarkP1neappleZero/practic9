n = int(input("Введите натуральное число n: "))
current = 1

while current <= n:
    skip_number = False
    #Проверка, попадает ли число в диапазондля пропуска
    if 5 <= current <= 9:
        skip_number = True
    elif 17 <= current <= 37:
        skip_number = True
    elif 78 <= current <= 87:
        skip_number = True
    #Пропускаем число 
    if skip_number:
        current += 1
        continue
    print(f"Число: {current}")
    current += 1
