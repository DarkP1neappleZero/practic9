price = int(input("Введите цену улсуги ведьмака: "))
#Список номиналов
coin = [25, 10, 5, 1]
total = 0
#Индекс для перебора номиналов 
i = 0
#Пока не выплатили сумму перебираем номиналы монет от крупных до мелких
while price > 0:
    while price >= coin[i]:
        price -= coin[i]
        total += 1

    i = i + 1
#Если перебрали номинал 4 монеты то выходим
    if i == 4:
        break

print(f"Минимальное количество монет: {total}")


